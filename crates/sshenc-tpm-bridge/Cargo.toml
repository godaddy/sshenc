[package]
name = "sshenc-tpm-bridge"
version.workspace = true
edition.workspace = true
rust-version.workspace = true

[[bin]]
name = "sshenc-tpm-bridge"
path = "src/main.rs"

[lints]
workspace = true

[dependencies]
hardware-enclave = { workspace = true }
# BridgeServer is not in hardware-enclave's public API; keep as direct path dep.
enclaveapp-tpm-bridge = { path = "../../../libenclaveapp/crates/enclaveapp-tpm-bridge" }
