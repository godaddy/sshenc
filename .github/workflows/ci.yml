name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    uses: jgowdy/libenclaveapp/.github/workflows/reusable-ci.yml@main
    with:
      platforms: '["macos-latest", "windows-latest"]'
